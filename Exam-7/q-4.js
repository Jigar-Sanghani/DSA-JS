


const arr_data = (arr, low = 0, high = arr.length - 1) => {

    if (low > high) {
        return -1;
    }

    let mid = Math.floor((low + high) / 2);
}


let arr = [7, 8, 10]

let check = arr_data(arr)

console.log(check);
